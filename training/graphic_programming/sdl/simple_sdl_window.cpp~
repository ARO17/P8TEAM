/*
 *
 * simple_sdl_window.cpp
 *
 * g++ -o simple_sdl_window simple_sdl_window.cpp -lSDL
 *
 */

#include "SDL/SDL.h"

int main( int argc, char* args[] ) {
  //Demarrer SDL
  SDL_Init( SDL_INIT_EVERYTHING );
  //Quitter SDL
  SDL_Quit();
  return 0;
}
